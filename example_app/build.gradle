plugins {
    id 'fr.stardustenterprises.rust.wrapper' version '2.1.0'
}

rust {
    command = "cargo"

    outputs = [
            '': 'example_app'
    ]

    outputDirectory = "bin/"

    profile = "release"
}
