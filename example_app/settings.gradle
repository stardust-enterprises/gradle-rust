pluginManagement {
    repositories {
        // Fetch plugin from mavenLocal if possible
        // to aid in plugin development.
        mavenLocal()
        gradlePluginPortal()
    }
}

include 'native', 'app'
